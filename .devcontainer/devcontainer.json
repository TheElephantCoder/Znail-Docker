{
  "name": "Znail Codespace (Stable Image)",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": { "moby": false }
  },
  "forwardPorts": [5000],
  "portsAttributes": { "5000": { "label": "Znail UI", "onAutoForward": "openBrowser" } },
  "postCreateCommand": "docker pull ghcr.io/theelephantcoder/znail-docker:latest",
  "postStartCommand": "docker rm -f znail >/dev/null 2>&1 || true && docker run -d --name znail -p 5000:5000 --restart unless-stopped ghcr.io/theelephantcoder/znail-docker:latest || true",
  "customizations": {
    "vscode": { "extensions": ["ms-azuretools.vscode-docker", "github.vscode-github-actions"] }
  },
  "remoteUser": "vscode"
}
