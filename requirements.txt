# requirements.txt

# Core web framework
Flask==2.3.3

# REST API (drop-in replacement for flask-restplus)
flask-restx==1.3.0

# Gunicorn WSGI server
gunicorn==21.2.0

# Other common deps (if not already pulled in by flask-restx)
Werkzeug==2.3.7
itsdangerous==2.1.2
Jinja2==3.1.2
click==8.1.7
